import RegisterForm from "../components/RegisterPage";

function RegisterPage() {
  return (
    <main className="min-h-screen bg-gradient-to-br from-primary/10 via-background to-secondary/10 flex items-center justify-center p-4 bg-zinc-400">
      <RegisterForm />
    </main>
  );
}

export default RegisterPage;
